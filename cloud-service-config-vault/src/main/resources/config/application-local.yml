debug: false
server:
  port: 8888
spring:
  profiles: local #本地环境
  cloud:
    config:
      server:
         vault:
          default-key: application
logging:
  level:
    org.springframework: 'DEBUG'
